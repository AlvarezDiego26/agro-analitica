# UI

Frontend base en Next.js App Router con enfoque feature-driven.

Principios:

- mobile-first
- responsive desde el inicio
- `app/` solo define rutas
- `features/` encapsula cada dominio visual
- `components/` contiene piezas compartidas
- `providers/`, `store/` y `lib/` centralizan infraestructura de UI

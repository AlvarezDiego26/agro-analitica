import { ProfileScreen } from "../../../features/perfil/components/profile-screen";

export const metadata = {
  title: "Mi Perfil | AgroAnalítica",
  description: "Configuración de tu cuenta en AgroAnalítica",
};

export default function ProfilePage() {
  return <ProfileScreen />;
}
